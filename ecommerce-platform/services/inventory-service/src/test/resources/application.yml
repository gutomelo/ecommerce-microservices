spring:
  cloud:
    config:
      enabled: false

platform:
  security:
    jwt:
      secret: test-secret-key-with-at-least-32-characters!!
      issuer: inventory-service-test
  outbox:
    publish-interval-millis: 500
