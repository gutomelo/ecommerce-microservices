spring:
  cloud:
    config:
      enabled: false

platform:
  security:
    jwt:
      secret: test-secret-key-with-at-least-32-characters!!
      issuer: payment-service-test
  payment:
    approval-threshold: 500.00
  outbox:
    publish-interval-millis: 500
